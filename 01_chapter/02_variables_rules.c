// first character must be a alphabet or underscore
// no commas, blanks are allowed
// no special symbol other than (_) are allowed 
// variable names are case sensitive means language treat lowercase and uppercase alphabets diffrent.

#include<stdio.h>

int main(){
    int a;
    int manish;
    int Manish;
    int _;
    _ = 7;
    printf("%d", _);
    int manish_churnia;
    return 0;
}